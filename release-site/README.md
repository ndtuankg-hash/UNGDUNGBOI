# B Dịch update manifest

Tải `version.json` lên dự án Cloudflare Pages `boi-ungdung1` mỗi khi phát hành phiên bản mới.
APK được lưu ở nơi hỗ trợ tệp lớn; trường `apkUrl` phải là đường dẫn HTTPS trực tiếp.
Ứng dụng xác minh `sha256` trước khi mở trình cài đặt Android.
